需要做一个用户名查询网站，模仿https://whatismyname.net/做一个一样的用户名查询网站，需要在用户浏览器前端输入用户名传递到vercel后端查询，并显示结果。

根据我们讨论的**阶段一 (Vercel 免费方案)** 架构，下面是你的项目所需的**完整代码**。

这个实现包括：

1. **Next.js (App Router)** + **Tailwind CSS** 构建的前端。
2. **Vercel Serverless Function** 作为后端 API (`/api/check`)。
3. **Vercel Postgres** 日志记录 (IP, UserAgent, 查询词)。
4. **集成 `wmn-data.json`**，并通过分类过滤来规避 Vercel 的 10 秒超时。
5. **并行查询** (`Promise.allSettled`) 和 **8 秒超时** 以确保稳定性。

------



### 🗂️ 项目文件结构



```
/
├── app/
│   ├── api/
│   │   └── check/
│   │       └── route.ts         # (5. 核心后端API)
│   ├── layout.tsx             # (2. 根布局 - Tailwind)
│   ├── page.tsx               # (4. 核心前端UI)
│   └── globals.css            # (3. Tailwind CSS)
├── lib/
│   ├── db.ts                  # (6. Vercel Postgres 配置)
│   └── types.ts               # (7. TypeScript 类型)
├── package.json               # (1. 项目依赖)
├── tailwind.config.ts
├── postcss.config.js
└── .env.local                 # (重要：Vercel环境变量)
```

------



### 🔧 部署与配置 (最后一步)



1. **Vercel 项目设置：**

   - 将你的代码推送到 GitHub，并导入到 Vercel。

2. **创建数据库：**

   - 在 Vercel 仪表盘，转到 "Storage" 标签页。
   - 创建一个 "Vercel Postgres" 数据库。
   - "Connect" 你的项目。Vercel 会自动设置 `POSTGRES_URL` 和其他环境变量。

3. **创建日志表：**

   - 在 Vercel 的 Postgres 数据库仪表盘中，找到 "Query" 标签页。
   - **运行以下 SQL 命令** 来创建我们的日志表：

   SQL

   ```
   CREATE TABLE query_logs (
     id SERIAL PRIMARY KEY,
     timestamp TIMESTAMPTZ DEFAULT NOW(),
     username_queried VARCHAR(255) NOT NULL,
     ip_address VARCHAR(100),
     user_agent TEXT
   );
   ```

4. **部署！**

   - 推送你的代码（或重新部署），你的网站现在应该可以工作了！
   - 当用户查询时，你可以在 Vercel 的 "Logs" 标签页看到 `console.error`，在 "Storage" 标签页看到你的 `query_logs` 表被填充。